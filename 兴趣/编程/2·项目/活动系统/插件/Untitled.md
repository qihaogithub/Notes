资源标记下的标签点击后应该是以模态弹窗形式显示名称输入，而不是底部出现。设置资源标记后，当前资源标签应该高亮回显，变支持重新选择其他资源类型。设置资源标记后“资源标记”标题右侧回显资源名称，名称旁边显示编辑按钮，点击编辑按钮也还是以模态弹窗形式显示名称输入框二次编辑。

交互标记和资源标记展示为tab切换按钮，因为他们只能选择一种，不可以同时编辑。例如用户添加资源标记后，切换标记类型，再次选择交互标记，需要弹窗二次确认，提示用户添加交互标记会使资源标记失效。

- 设为切图：我希望是一个通栏按钮，位于顶部。需要注意的是设为截图后，同时还能继续添加资源标记。但无法添加交互标记。
- 资源标记：“资源标记”标题，标题右侧显示资源名称设置按钮，双击或点击右侧编辑按钮直接原位输入。标题下面一排标签按钮（可选择“图片、文本、色值、视频、Lottie、Svga、Unity”），用户直接点选，选中如果发现用户没有输入资源名称，则弹窗展示输入框。二次修改是可以通过标题右侧资源名称设置按钮。
- 交互标记：“交互标记”标题，标题下面一排标签按钮（纵向列表、横向列表、自由画布）
- 添加提示词：直接显示提示词输入框，不需要标题和“\+ 创建 AI 指令层”按钮，用户输入框输入自动添加提示词图层，并会随编辑自动更新。
- 优化建议：优化建议我希望能直接展示结果，不需要用户点击
