# Go Fractals CLI - 设计

## 概述
一个生成 ASCII 艺术分形的命令行工具。支持两种分形类型，且输出可配置。

## 指令
- `sierpinski`：使用递归细分生成谢尔宾斯基三角形。
- `mandelbrot`：将曼德博集合渲染为 ASCII 艺术。

## 验收标准
1. `fractals --help` 显示用法。
2. 生成的分形具有辨识度。
3. 各种参数（大小、高度、迭代次数等）能正常工作。
4. 所有测试通过。
