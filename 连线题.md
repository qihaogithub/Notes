#### 一、 玩法规则
1. **基础连线**：支持单阶（2 列）或多阶（最多 4 列）连线。
   * **连线方向**：支持双向连线（既可以从左向右连，也可以逆向从右向左连）。
   * **跨列限制**：必须在相邻的两列之间进行连线，**严禁跨列连线**。
2. **正误判断**：**连一个就判对/错一个**。无需等待全部连接完成，当用户连成一根线时，系统立即给出正误反馈。
3. **AI 数字人**：**无 AI 功能**，两行选项可考虑是否支持。
4. **矩阵限制**：最大支持 **4 列 × 4 行**，最小支持 **2 列 × 2 行**。

#### 二、 交互说明
1. **连线动作**：用户在选项左右两侧的“连接点”按下并拖拽生成连线，拖至目标“连接点”松手建立连接。
2. **交互反馈**：
   * **拖拽中**：默认灰色线条。
   * **连线正确**：线条变为 **绿色实线**，并持续保留。
   * **连线错误**：
     * **线条变化**：线条变为 **红色实线**，展示 **1 秒后自动断开**（消失）。
     * **抖动反馈**：判定错误时，**被连的目标选项**触发左右抖动动效，提示用户连接错误。

#### 三、 尺寸与排版适配规则
**1. 整体布局**
* 所有选项组成的矩阵在父级容器整体内保持**上下居中、左右居中**

**2. 选项尺寸与列间距适配**
* **高度固定**：所有行数、列数组合下，选项高度统一为  `130px` **。
* **宽度与列间距（固定值，不自动分配）**：
  * **2 列** 模式：选项宽度  `400px` **，列间距  `340px` 
  * **3 列** 模式：选项宽度  `320px` **，列间距  `200px` 
  * **4 列** 模式：选项宽度  `228px` **，列间距  `148px` 

**3. 行间距适配**
* **2 行** 模式：行间距  `100px`
* **3 行** 模式：行间距  `80px` 
* **4 行** 模式：行间距  `24px` 

#### 四、 内容限制与展示规则
1. **题干/标题展示**：
   * 最多显示一行，当文字超出容器宽度时，显示缓慢滚动（跑马灯）效果。
1. **选项类型**：
   * 纯文字、纯图片、纯语音三种，**不能组合**
1. **选项文字排版：
   * 文字大小限制在 `24px` 到 `36px` 之间。
   * **处理逻辑**：当文字较多、超出当前选项宽度时，**优先缩小字号**进行适配；当字号缩小至下限 `24px` 后若仍无法单行展示，**再进行换行处理**。