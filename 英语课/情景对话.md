---
创建日期: 2026-04-26T10:43:32+08:00
修改日期: 2026-04-26T11:29:01+08:00
---
## 功能简介
通过摄像头采集用户头像，模拟用户在虚拟场景中，与AI 扮演的角色进行语音对话，提升英语口语表达能力。

## 交互逻辑
### 题目判定规则
* 满足规则A **或** 规则B  
* **规则 A**：命中词库中 80% 以上的关键词。
*   **规则 B**：交互回复次数达到 N 次。

### 题目作答流程
1.  **加载阶段**：显示 Loading 动画，完成后进入场景。
2.  **触发阶段**：AI 角色率先发出引导语，触发 AI 角色“说话”动画。
3.  **聆听阶段**：按钮显示“待机（点击唤起）” -> “说话（录音中）”->结束说话（点击按钮或系统识别到长时间停顿）->“画面中对话框展示转录文本”
4.  **响应阶段**：用户说完后，系统判断后由指定 NPC 回复。
5.  **超时处理**：
    *   **催促机制**：若 10 s 内无响应，AI 角色触发催促语。
    *   **跳过机制**：连续 3 次超时未作答，屏幕出现“跳过”按钮。

###  角色动画状态机
*   **待机：轻微呼吸感/晃动动画。
*   **说话：嘴部开合动画，配合语音播放。
*   **欢呼：过关时触发，如跳跃、挥手或表情变大。

###  对话框：
- 用户与NPC起泡样式不同。
- 气泡内仅展示图标（表示语音）
- 或根据逻辑优化为无文字纯听力模式。

## 后台配置
*   **场景背景**：支持上传背景图（PNG/JPG 格式）。
*   **人物组合**：支持选择 2-4 个角色（预置资源库）。
*   **角色定义**：
    *   指定其中 1 个角色为“用户”（头像显示为用户上传的照片）。
    *   指定 AI 角色序列及其对应的话语逻辑。
*   **对话脚本**：配置故事大纲、引导语、过关词库、催促话术。
*   **NPC 位置**：支持自定义各 NPC 在场景中的排列顺序（座位 1-4）。


有一个人物是用户，npc 引导用户，npc 多状态动画，纯语音，没有文字。所有人物配置固定，配置只换背景和 npc，npc 位置可以选几号位置。

背景图是后台配置的场景图
提前预置好多个人物
后台配置故事内容大纲介绍
人物是后台配置的 2-4 个固定顺序的人物
其中指定一个人为用户。
进入后，由指定 AI 发出初始话语，进入场
景。用户发言后，由 AI 判断使用指定人物
进行回复。

人物有三个动画状态：待机、说话、欢呼


过关条件（满足任意 1 个）：
1、用户说出配置过关词库里 80%的单词
2、用户说了 N 次以上回复
播放配置的 NPC 引导结束语，播放欢呼动
画，过关

AI 说出引导语后，用户超过 10 秒没有回
答，告诉 AI 来一个句子催他一下
超过连续 3 词没回答，出现一个跳过按钮

4 个角色形象
背景图后台上传
进场景前需要加载动画